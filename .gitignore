node_modules/
dist/
.env
.env.local
.env.txt
*.log
.vercel

# Secrets — never commit
CREDENTIALS-INVENTORY.md
*.jks
key.properties
# Local Claude settings — can contain keys in one-off command allow-rules
.claude/settings.local.json

# Local-only
_trash/

# Claude Code agent worktrees (each is its own git repo — must not be embedded)
.claude/worktrees/

# Editor / OS lock files
~$*
.idea/

# Generated PowerPoint output (regen via scripts when needed)
~$*.pptx
